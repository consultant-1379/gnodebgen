/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.2.4, Date: 05-Aug-2021.
 */
package asn.lte.generated.X2AP_PDU_Contents;

import com.objsys.asn1j.runtime.*;
import asn.lte.generated.X2AP_IEs.ECGI;
import asn.lte.generated.X2AP_IEs.ServedCell_Information;
import asn.lte.generated.X2AP_IEs.Neighbour_Information;

public class ServedCellsToModify_Item extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_X2AP_PDU_ContentsRtkey._rtkey);
   }

   public String getAsn1TypeName()  {
      return "ServedCellsToModify-Item";
   }

   public ECGI old_ecgi;
   public ServedCell_Information servedCellInfo;
   public Neighbour_Information neighbour_Info;  // optional
   public ServedCellsToModify_Item_iE_Extensions iE_Extensions;  // optional
   public Asn1OpenExt extElem1;

   public ServedCellsToModify_Item () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public ServedCellsToModify_Item (
      ECGI old_ecgi_,
      ServedCell_Information servedCellInfo_,
      Neighbour_Information neighbour_Info_,
      ServedCellsToModify_Item_iE_Extensions iE_Extensions_
   ) {
      super();
      old_ecgi = old_ecgi_;
      servedCellInfo = servedCellInfo_;
      neighbour_Info = neighbour_Info_;
      iE_Extensions = iE_Extensions_;
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public ServedCellsToModify_Item (
      ECGI old_ecgi_,
      ServedCell_Information servedCellInfo_
   ) {
      super();
      old_ecgi = old_ecgi_;
      servedCellInfo = servedCellInfo_;
   }

   public void init () {
      old_ecgi = null;
      servedCellInfo = null;
      neighbour_Info = null;
      iE_Extensions = null;
      extElem1 = null;
   }

   public int getElementCount() { return 5; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return old_ecgi;
         case 1: return servedCellInfo;
         case 2: return neighbour_Info;
         case 3: return iE_Extensions;
         case 4: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "old-ecgi";
         case 1: return "servedCellInfo";
         case 2: return "neighbour-Info";
         case 3: return "iE-Extensions";
         case 4: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean neighbour_InfoPresent = buffer.decodeBit ("neighbour_InfoPresent");
      boolean iE_ExtensionsPresent = buffer.decodeBit ("iE_ExtensionsPresent");

      // decode old_ecgi

      buffer.getContext().eventDispatcher.startElement("old_ecgi", -1);

      old_ecgi = new ECGI();
      old_ecgi.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("old_ecgi", -1);

      // decode servedCellInfo

      buffer.getContext().eventDispatcher.startElement("servedCellInfo", -1);

      servedCellInfo = new ServedCell_Information();
      servedCellInfo.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("servedCellInfo", -1);

      // decode neighbour_Info

      if (neighbour_InfoPresent) {
         buffer.getContext().eventDispatcher.startElement("neighbour_Info", -1);

         neighbour_Info = new Neighbour_Information();
         neighbour_Info.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("neighbour_Info", -1);
      }
      else {
         neighbour_Info = null;
      }

      // decode iE_Extensions

      if (iE_ExtensionsPresent) {
         buffer.getContext().eventDispatcher.startElement("iE_Extensions", -1);

         iE_Extensions = new ServedCellsToModify_Item_iE_Extensions();
         iE_Extensions.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("iE_Extensions", -1);
      }
      else {
         iE_Extensions = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);

                  buffer.invokeCharacters (openType.toString());
                  buffer.getContext().eventDispatcher.endElement("...", -1);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void print (java.lang.StringBuilder _sb, String _varName, int _level)
   {
      indent (_sb, _level);
      _sb.append(_varName).append(" {\n");
      if (old_ecgi != null) old_ecgi.print (_sb, "old_ecgi", _level+1);
      if (servedCellInfo != null) servedCellInfo.print (_sb, "servedCellInfo", _level+1);
      if (neighbour_Info != null) neighbour_Info.print (_sb, "neighbour_Info", _level+1);
      if (iE_Extensions != null) iE_Extensions.print (_sb, "iE_Extensions", _level+1);
      if (extElem1 != null) extElem1.print (_sb, "extElem1", _level+1);
      indent (_sb, _level);
      _sb.append ("}\n");
   }

}
