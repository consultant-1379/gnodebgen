/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.2.4, Date: 05-Aug-2021.
 */
package asn.lte.generated.EUTRA_RRC_Definitions;

import com.objsys.asn1j.runtime.*;

public class PDCP_Config_headerCompression_rohc extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_EUTRA_RRC_DefinitionsRtkey._rtkey);
   }

   public String getAsn1TypeName()  {
      return "SEQUENCE";
   }

   public Asn1Integer maxCID;  // default = 15()
   public PDCP_Config_headerCompression_rohc_profiles profiles;
   public Asn1OpenExt extElem1;

   public PDCP_Config_headerCompression_rohc () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public PDCP_Config_headerCompression_rohc (
      Asn1Integer maxCID_,
      PDCP_Config_headerCompression_rohc_profiles profiles_
   ) {
      super();
      maxCID = maxCID_;
      profiles = profiles_;
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public PDCP_Config_headerCompression_rohc (
      PDCP_Config_headerCompression_rohc_profiles profiles_
   ) {
      super();
      maxCID = new Asn1Integer (15);
      profiles = profiles_;
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public PDCP_Config_headerCompression_rohc (long maxCID_,
      PDCP_Config_headerCompression_rohc_profiles profiles_
   ) {
      super();
      maxCID = new Asn1Integer (maxCID_);
      profiles = profiles_;
   }

   public void init () {
      maxCID = new Asn1Integer (15);
      profiles = null;
      extElem1 = null;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return maxCID;
         case 1: return profiles;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "maxCID";
         case 1: return "profiles";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean maxCIDPresent = buffer.decodeBit ("maxCIDPresent");

      // decode maxCID

      if (maxCIDPresent) {
         buffer.getContext().eventDispatcher.startElement("maxCID", -1);

         maxCID = new Asn1Integer();
         maxCID.decode (buffer, 1, 16383);

         buffer.invokeCharacters(maxCID.toString());
         buffer.getContext().eventDispatcher.endElement("maxCID", -1);
      }
      else {
         maxCID = new Asn1Integer();
         maxCID.value = 15;
      }

      // decode profiles

      buffer.getContext().eventDispatcher.startElement("profiles", -1);

      profiles = new PDCP_Config_headerCompression_rohc_profiles();
      profiles.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("profiles", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);

                  buffer.invokeCharacters (openType.toString());
                  buffer.getContext().eventDispatcher.endElement("...", -1);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void print (java.lang.StringBuilder _sb, String _varName, int _level)
   {
      indent (_sb, _level);
      _sb.append(_varName).append(" {\n");
      if (maxCID != null) maxCID.print (_sb, "maxCID", _level+1);
      if (profiles != null) profiles.print (_sb, "profiles", _level+1);
      if (extElem1 != null) extElem1.print (_sb, "extElem1", _level+1);
      indent (_sb, _level);
      _sb.append ("}\n");
   }

}
