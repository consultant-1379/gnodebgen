/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.2.4, Date: 05-Aug-2021.
 */
package asn.nr.generated.NR_RRC_Definitions;

import com.objsys.asn1j.runtime.*;

public class DMRS_UplinkConfig_transformPrecodingDisabled extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_NR_RRC_DefinitionsRtkey._rtkey);
   }

   public String getAsn1TypeName()  {
      return "SEQUENCE";
   }

   public Asn1Integer scramblingID0;  // optional
   public Asn1Integer scramblingID1;  // optional
   public boolean mV2ExtPresent;
   public DMRS_UplinkConfig_transformPrecodingDisabled_dmrs_Uplink_r16 dmrs_Uplink_r16 = null;  // optional
   public Asn1OpenExt extElem1;

   public DMRS_UplinkConfig_transformPrecodingDisabled () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public DMRS_UplinkConfig_transformPrecodingDisabled (
      Asn1Integer scramblingID0_,
      Asn1Integer scramblingID1_,
      DMRS_UplinkConfig_transformPrecodingDisabled_dmrs_Uplink_r16 dmrs_Uplink_r16_
   ) {
      super();
      scramblingID0 = scramblingID0_;
      scramblingID1 = scramblingID1_;
      dmrs_Uplink_r16 = dmrs_Uplink_r16_;
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public DMRS_UplinkConfig_transformPrecodingDisabled (long scramblingID0_,
      long scramblingID1_,
      DMRS_UplinkConfig_transformPrecodingDisabled_dmrs_Uplink_r16 dmrs_Uplink_r16_
   ) {
      super();
      scramblingID0 = new Asn1Integer (scramblingID0_);
      scramblingID1 = new Asn1Integer (scramblingID1_);
      dmrs_Uplink_r16 = dmrs_Uplink_r16_;
   }

   public void init () {
      scramblingID0 = null;
      scramblingID1 = null;
      dmrs_Uplink_r16 = null;
      extElem1 = null;
   }

   public int getElementCount() { return 4; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return scramblingID0;
         case 1: return scramblingID1;
         case 2: return dmrs_Uplink_r16;
         case 3: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "scramblingID0";
         case 1: return "scramblingID1";
         case 2: return "dmrs-Uplink-r16";
         case 3: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean scramblingID0Present = buffer.decodeBit ("scramblingID0Present");
      boolean scramblingID1Present = buffer.decodeBit ("scramblingID1Present");

      // decode scramblingID0

      if (scramblingID0Present) {
         buffer.getContext().eventDispatcher.startElement("scramblingID0", -1);

         scramblingID0 = new Asn1Integer();
         scramblingID0.decode (buffer, 0, 65535);

         buffer.invokeCharacters(scramblingID0.toString());
         buffer.getContext().eventDispatcher.endElement("scramblingID0", -1);
      }
      else {
         scramblingID0 = null;
      }

      // decode scramblingID1

      if (scramblingID1Present) {
         buffer.getContext().eventDispatcher.startElement("scramblingID1", -1);

         scramblingID1 = new Asn1Integer();
         scramblingID1.decode (buffer, 0, 65535);

         buffer.invokeCharacters(scramblingID1.toString());
         buffer.getContext().eventDispatcher.endElement("scramblingID1", -1);
      }
      else {
         scramblingID1 = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         if (i < bitcnt) bitmap[i++] = buffer.decodeBit ("mV2ExtPresent");

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode extension group

         if (i < bitcnt && bitmap[i++]) {
            mV2ExtPresent = true;
            bitidx = buffer.decodeExtLength ();

            boolean dmrs_Uplink_r16Present = buffer.decodeBit ("dmrs_Uplink_r16Present");

            // decode dmrs_Uplink_r16

            if (dmrs_Uplink_r16Present) {
               buffer.getContext().eventDispatcher.startElement("dmrs_Uplink_r16", -1);

               int tval = DMRS_UplinkConfig_transformPrecodingDisabled_dmrs_Uplink_r16.decodeEnumValue (buffer);
               dmrs_Uplink_r16 = DMRS_UplinkConfig_transformPrecodingDisabled_dmrs_Uplink_r16.valueOf (tval);
               buffer.getContext().eventDispatcher.endElement("dmrs_Uplink_r16", -1);
            }
            else {
               dmrs_Uplink_r16 = null;
            }

            buffer.moveBitCursor (bitidx);
         }

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);

                  buffer.invokeCharacters (openType.toString());
                  buffer.getContext().eventDispatcher.endElement("...", -1);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (mV2ExtPresent ||
      ((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((scramblingID0 != null), null);
      buffer.encodeBit ((scramblingID1 != null), null);

      // encode scramblingID0

      if (scramblingID0 != null) {
         buffer.getContext().eventDispatcher.startElement("scramblingID0", -1);

         scramblingID0.encode (buffer, 0, 65535);

         buffer.getContext().eventDispatcher.endElement("scramblingID0", -1);
      }

      // encode scramblingID1

      if (scramblingID1 != null) {
         buffer.getContext().eventDispatcher.startElement("scramblingID1", -1);

         scramblingID1.encode (buffer, 0, 65535);

         buffer.getContext().eventDispatcher.endElement("scramblingID1", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 1;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         buffer.encodeBit (mV2ExtPresent, null);

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         Asn1PerEncodeBuffer mainBuffer = buffer;
         buffer = new Asn1PerEncodeBuffer (buffer.isAligned());

         if (mV2ExtPresent) {
            buffer.reset();
            buffer.encodeBit ((dmrs_Uplink_r16 != null), "optbit");
            if (dmrs_Uplink_r16 != null) {
               dmrs_Uplink_r16.encode (buffer);
            }
            mainBuffer.encodeOpenType (buffer, null);
         }

         buffer = mainBuffer;

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

   public void print (java.lang.StringBuilder _sb, String _varName, int _level)
   {
      indent (_sb, _level);
      _sb.append(_varName).append(" {\n");
      if (scramblingID0 != null) scramblingID0.print (_sb, "scramblingID0", _level+1);
      if (scramblingID1 != null) scramblingID1.print (_sb, "scramblingID1", _level+1);
      if (dmrs_Uplink_r16 != null) dmrs_Uplink_r16.print (_sb, "dmrs_Uplink_r16", _level+1);
      if (extElem1 != null) extElem1.print (_sb, "extElem1", _level+1);
      indent (_sb, _level);
      _sb.append ("}\n");
   }

}
