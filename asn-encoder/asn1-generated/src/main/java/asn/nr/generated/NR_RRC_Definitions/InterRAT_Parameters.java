/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.2.4, Date: 05-Aug-2021.
 */
package asn.nr.generated.NR_RRC_Definitions;

import com.objsys.asn1j.runtime.*;

public class InterRAT_Parameters extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_NR_RRC_DefinitionsRtkey._rtkey);
   }

   public String getAsn1TypeName()  {
      return "InterRAT-Parameters";
   }

   public EUTRA_Parameters eutra;  // optional
   public boolean mV2ExtPresent;
   public UTRA_FDD_Parameters_r16 utra_FDD_r16;  // optional
   public Asn1OpenExt extElem1;

   public InterRAT_Parameters () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public InterRAT_Parameters (
      EUTRA_Parameters eutra_,
      UTRA_FDD_Parameters_r16 utra_FDD_r16_
   ) {
      super();
      eutra = eutra_;
      utra_FDD_r16 = utra_FDD_r16_;
   }

   public void init () {
      eutra = null;
      utra_FDD_r16 = null;
      extElem1 = null;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return eutra;
         case 1: return utra_FDD_r16;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "eutra";
         case 1: return "utra-FDD-r16";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean eutraPresent = buffer.decodeBit ("eutraPresent");

      // decode eutra

      if (eutraPresent) {
         buffer.getContext().eventDispatcher.startElement("eutra", -1);

         eutra = new EUTRA_Parameters();
         eutra.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("eutra", -1);
      }
      else {
         eutra = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         if (i < bitcnt) bitmap[i++] = buffer.decodeBit ("mV2ExtPresent");

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode extension group

         if (i < bitcnt && bitmap[i++]) {
            mV2ExtPresent = true;
            bitidx = buffer.decodeExtLength ();

            boolean utra_FDD_r16Present = buffer.decodeBit ("utra_FDD_r16Present");

            // decode utra_FDD_r16

            if (utra_FDD_r16Present) {
               buffer.getContext().eventDispatcher.startElement("utra_FDD_r16", -1);

               utra_FDD_r16 = new UTRA_FDD_Parameters_r16();
               utra_FDD_r16.decode (buffer);
               buffer.getContext().eventDispatcher.endElement("utra_FDD_r16", -1);
            }
            else {
               utra_FDD_r16 = null;
            }

            buffer.moveBitCursor (bitidx);
         }

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);

                  buffer.invokeCharacters (openType.toString());
                  buffer.getContext().eventDispatcher.endElement("...", -1);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (mV2ExtPresent ||
      ((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((eutra != null), null);

      // encode eutra

      if (eutra != null) {
         buffer.getContext().eventDispatcher.startElement("eutra", -1);

         eutra.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("eutra", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 1;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         buffer.encodeBit (mV2ExtPresent, null);

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         Asn1PerEncodeBuffer mainBuffer = buffer;
         buffer = new Asn1PerEncodeBuffer (buffer.isAligned());

         if (mV2ExtPresent) {
            buffer.reset();
            buffer.encodeBit ((utra_FDD_r16 != null), "optbit");
            if (utra_FDD_r16 != null) {
               utra_FDD_r16.encode (buffer);
            }
            mainBuffer.encodeOpenType (buffer, null);
         }

         buffer = mainBuffer;

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

   public void print (java.lang.StringBuilder _sb, String _varName, int _level)
   {
      indent (_sb, _level);
      _sb.append(_varName).append(" {\n");
      if (eutra != null) eutra.print (_sb, "eutra", _level+1);
      if (utra_FDD_r16 != null) utra_FDD_r16.print (_sb, "utra_FDD_r16", _level+1);
      if (extElem1 != null) extElem1.print (_sb, "extElem1", _level+1);
      indent (_sb, _level);
      _sb.append ("}\n");
   }

}
