/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.2.4, Date: 05-Aug-2021.
 */
package asn.nr.generated.NR_RRC_Definitions;

import com.objsys.asn1j.runtime.*;

public class RRCRelease_IEs_deprioritisationReq extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_NR_RRC_DefinitionsRtkey._rtkey);
   }

   public String getAsn1TypeName()  {
      return "SEQUENCE";
   }

   public RRCRelease_IEs_deprioritisationReq_deprioritisationType deprioritisationType = null;
   public RRCRelease_IEs_deprioritisationReq_deprioritisationTimer deprioritisationTimer = null;

   public RRCRelease_IEs_deprioritisationReq () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public RRCRelease_IEs_deprioritisationReq (
      RRCRelease_IEs_deprioritisationReq_deprioritisationType deprioritisationType_,
      RRCRelease_IEs_deprioritisationReq_deprioritisationTimer deprioritisationTimer_
   ) {
      super();
      deprioritisationType = deprioritisationType_;
      deprioritisationTimer = deprioritisationTimer_;
   }

   public void init () {
      deprioritisationType = null;
      deprioritisationTimer = null;
   }

   public int getElementCount() { return 2; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return deprioritisationType;
         case 1: return deprioritisationTimer;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "deprioritisationType";
         case 1: return "deprioritisationTimer";
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // decode deprioritisationType

      buffer.getContext().eventDispatcher.startElement("deprioritisationType", -1);

      {
         int tval = RRCRelease_IEs_deprioritisationReq_deprioritisationType.decodeEnumValue (buffer);
         deprioritisationType = RRCRelease_IEs_deprioritisationReq_deprioritisationType.valueOf (tval);
      }
      buffer.getContext().eventDispatcher.endElement("deprioritisationType", -1);

      // decode deprioritisationTimer

      buffer.getContext().eventDispatcher.startElement("deprioritisationTimer", -1);

      {
         int tval = RRCRelease_IEs_deprioritisationReq_deprioritisationTimer.decodeEnumValue (buffer);
         deprioritisationTimer = RRCRelease_IEs_deprioritisationReq_deprioritisationTimer.valueOf (tval);
      }
      buffer.getContext().eventDispatcher.endElement("deprioritisationTimer", -1);

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // encode deprioritisationType

      if (deprioritisationType != null) {
         buffer.getContext().eventDispatcher.startElement("deprioritisationType", -1);

         deprioritisationType.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("deprioritisationType", -1);
      }
      else throw new Asn1MissingRequiredException ("deprioritisationType");

      // encode deprioritisationTimer

      if (deprioritisationTimer != null) {
         buffer.getContext().eventDispatcher.startElement("deprioritisationTimer", -1);

         deprioritisationTimer.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("deprioritisationTimer", -1);
      }
      else throw new Asn1MissingRequiredException ("deprioritisationTimer");

   }

   public void print (java.lang.StringBuilder _sb, String _varName, int _level)
   {
      indent (_sb, _level);
      _sb.append(_varName).append(" {\n");
      if (deprioritisationType != null) deprioritisationType.print (_sb, "deprioritisationType", _level+1);
      if (deprioritisationTimer != null) deprioritisationTimer.print (_sb, "deprioritisationTimer", _level+1);
      indent (_sb, _level);
      _sb.append ("}\n");
   }

}
