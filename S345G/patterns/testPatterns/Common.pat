# Heartbeat = 1 /network function/min => 15 per ROP per NF, spread evenly over the number of ‘EBS-N Required’ Events
# 1  x DroppedEvent  event added at the end.
# Hoping to add 1 dropped event every minute.

id = Heartbeat
id = DroppedEvents